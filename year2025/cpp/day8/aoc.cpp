#include "aoc.h"
#include "../common/common.h"
#include <set>
#include <map>

using std::map;
using std::pair;
using std::set;
using std::stoll;
using std::vector;

long long part1(vector<string> lines)
{
    return -1;
}

long long part2(vector<string> lines)
{
    return -1;
}
